nan_value = float("nan")
# == nan

print(nan_value == float("nan"))

# What do you think the following code will output?

# False: normal identity comparisons nith nan don't compute. See py docs. [Where?...]
# Need to import math module; math.isnan()

# Had to look up answer for above.